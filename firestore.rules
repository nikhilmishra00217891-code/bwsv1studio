rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // TEMPORARY: Allow all reads and writes to all collections to diagnose permission issues.
    // This is NOT secure and should be replaced before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
